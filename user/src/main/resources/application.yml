server:
  port: 8082
spring:
  application:
    name: user
  datasource:
    password: 9@*Mqf293631
    url: jdbc:postgresql://localhost:5432/user
    username: payhez
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  kafka:
    bootstrap-servers: localhost:29092
  zipkin:
    base-url: http://localhost:9411